---
import Base from '../layouts/Base.astro';
import TopNav from '../components/TopNav.astro';
---

<Base
  title="404 — page not found"
  description="The page you were looking for doesn't exist."
>
  <TopNav />

  <main class="frame">
    <section class="hero">
      <div class="eyebrow">Error 404</div>
      <h1>Page not found</h1>
      <p class="lede">
        The page you were looking for doesn't exist, or has moved. No drama — just a missing path.
      </p>
    </section>

    <section class="links">
      <div class="eyebrow">Try one of these</div>
      <ul>
        <li><a href="/">Home</a> — landing page, work tiles, availability</li>
        <li><a href="/projects">Projects</a> — products + side projects</li>
        <li><a href="/projects/pipeforge">Pipeforge</a> — Claude Code orchestration walkthrough</li>
        <li><a href="/projects/kb">Knowledge base</a> — selected entries</li>
        <li><a href="/about">About</a> — background and availability</li>
      </ul>
    </section>
  </main>
</Base>

<style>
  .frame {
    max-width: 1140px;
    margin: 0 auto;
    padding: 0 32px;
  }

  .hero {
    padding: 80px 0 32px;
  }
  .eyebrow {
    font-family: var(--mono);
    font-size: 11.5px;
    color: var(--muted);
    letter-spacing: 0.06em;
    text-transform: uppercase;
    margin-bottom: 18px;
  }
  .hero h1 {
    font-family: var(--serif);
    font-weight: 500;
    font-size: 48px;
    line-height: 1.05;
    letter-spacing: -0.02em;
    color: var(--fg);
    margin-bottom: 18px;
  }
  .lede {
    font-family: var(--serif);
    font-weight: 500;
    font-size: 19px;
    line-height: 1.55;
    color: var(--fg);
    text-wrap: pretty;
    max-width: 660px;
  }

  .links {
    padding: 36px 0 80px;
    border-top: 1px solid var(--rule);
  }
  .links ul {
    list-style: none;
    padding: 0;
    margin: 0;
    font-family: var(--sans);
    font-size: 15px;
    line-height: 1.75;
    color: var(--fg);
  }
  .links li + li {
    margin-top: 4px;
  }
  .links a {
    color: var(--accent);
    text-decoration: underline;
    text-underline-offset: 3px;
    text-decoration-color: rgba(125, 179, 108, 0.33);
    margin-right: 6px;
  }

  @media (max-width: 640px) {
    .frame {
      padding: 0 22px;
    }
    .hero {
      padding: 48px 0 24px;
    }
    .hero h1 {
      font-size: 32px;
    }
    .lede {
      font-size: 16px;
    }
    .links {
      padding: 24px 0 48px;
    }
  }
</style>
